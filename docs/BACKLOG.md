# Rhythm — Бэклог техдолга и ожидающих задач

Статус: **только зафиксировано, НЕ чинить в текущей уборке.**
Каждый пункт выполняется отдельным коммитом после согласования с владельцем.

## UI
- **Переполнение контейнера палитрой «Тема таймлайна» на 1920×1080.**
  Настройки → выбор палитры выходит за границы карточки на широких экранах.

## Копирайты
- **Название и девиз на экране входа — на русском.**
  Текущая англоязычная формулировка; новую согласовать с владельцем.
- ~~**Убрать «end-to-end шифрование» из футера экрана входа.**~~
  ✅ Сделано (гигиена-спринт): заменено на «данные хранятся в твоём
  аккаунте и защищены политиками доступа (RLS)».

## Auth
- **OAuth Google / Apple.**
  Создать провайдеры в дашборде Supabase и настроить redirect URLs
  (см. docs/supabase-activation.md). Код-путь в supabaseProvider уже готов.

## CI
- **Строгий Prettier при появлении shell.**
  Сейчас шаг `Format check` неблокирующий (`continue-on-error: true`),
  т.к. нет терминала для `prettier --write`. После прогона `--write`
  и коммита результата вернуть строгость.
- **Node 20 → 24** в `.github/workflows/ci.yml`.
- **Путь `coverage/`** — проверить загрузку артефакта покрытия.

## Производительность
- **Code-splitting recharts.** ✅ Частично (гигиена-спринт): все экраны
  вкладок переведены на `React.lazy` — recharts автоматически выделяется
  в чанки Insights/MoodOverview, главный чанк < 500 КБ.
  Осталось: `build.rollupOptions.output.manualChunks` (правки
  vite.config.ts в гигиену не вошли — отдельный коммит).

## Качество кода
- **Вернуть правило `react-hooks/exhaustive-deps` (warn).**
  Сейчас выключено; после наведения порядка в зависимостях эффектов
  включить обратно как предупреждение.

## Архитектура (гигиена-спринт)
- **Расщепление God Context — после M0.**
  `store.tsx` вырос в единый провайдер (~1000 строк: auth, tasks, mood,
  sync, suggestions, prompts, deep links). Разбить на изолированные
  контроллеры/notifiers. Не делать до стабилизации мобильного трека,
  чтобы не дёргать общий контракт данных.
- **Декомпозиция TodayScreen / Shell / TaskModal.**
  Экраны-монолиты (700+ строк) разнести на подкомпоненты и хуки
  (таймлайн-блоки, Now-панель, секции модалки) без изменения поведения.

## Безопасность
- **Убедиться, что demo-хэш не активен в проде.**
  `demoHash` используется только в `localProvider` (демо-режим).
  Проверить в production: включён Supabase-провайдер, `passHash` не
  попадает в `user_profiles`, demo-ветка недостижима при заданных env.

## Интеграции
- **Google Calendar stub → реальный OAuth.**
  Заменить демо-`googleProvider` на Calendar API v3: consent-экран,
  двусторонняя синхронизация, очередь удалений (tombstones уже есть).

## Экспорт
- **PDF через jsPDF.**
  Сейчас отчёт — print-to-PDF (диалог печати). Заменить на клиентскую
  генерацию jsPDF с встроенным кириллическим шрифтом: офлайн-экспорт
  без системного диалога.

## Инструменты
- **Storybook.**
  Каталог переиспользуемых компонентов: MoodCheckInSheet, MoodTapestry,
  InsightCard, DialogShell, MoodFacePicker, JournalFiltersPanel.
